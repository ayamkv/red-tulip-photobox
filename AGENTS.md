# Project Guidelines

## Styling

- Avoid custom text, letter, and word spacing in CSS and component classes.
- Do not use `letter-spacing`, `word-spacing`, or Tailwind `tracking-*` utilities.
- Keep the typefaces' natural spacing unless the user explicitly requests otherwise.
